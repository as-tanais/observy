services:
  pgdb:
    image: "postgres:16"
    container_name: "pgdb_go_yandex"
    environment:
      - POSTGRES_USER=db_user
      - POSTGRES_PASSWORD=pwd123
    ports:
      - "54322:5432"
    volumes:
      - ./data:/var/lib/postgresql/data